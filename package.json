{
  "name": "kafka-samples",
  "version": "1.0.0",
  "private": true,
  "workspaces": [ 
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "new-order": "turbo run start --filter=new-order-service",
    "email": "turbo run start --filter=email-service",
    "log": "turbo run start --filter=log-service",
    "fraud-detector": "turbo run start --filter=fraud-detector-service"
  },
  "devDependencies": {
    "turbo": "2.0.12"
  },
  "packageManager": "pnpm@9.1.3"
}
